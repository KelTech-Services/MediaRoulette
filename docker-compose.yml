version: '3.8'

services:
  plexroulette:
    build: .
    container_name: plexroulette
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
    restart: unless-stopped
